case_id: unsupported-mechanic
description: The system does not model this. It is reported, not invented.
entities: [[pc-ada, Ada]]
present: []
turns:
  - proposal:
      actor_id: pc-ada
      intent: Ada phase-shifts through the floor
      proposed_action_type: phase_moon
    expect:
      disposition: unsupported
      expect_events: []
      response_contains_any: ["does not implement"]
