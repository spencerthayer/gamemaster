"""Document ingestion interface.

``DocumentIngestor`` with ``supports()``/``ingest()``; markdown, text, and
PDF first. Preserves source file, page, section, heading, doc id, content
pack, system id, edition/version, and visibility. Phase 19 of the execution
plan fills this in.
"""
