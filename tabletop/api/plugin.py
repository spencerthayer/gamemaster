"""GameSystemPlugin contract.

Defines the platform-agnostic game-system plugin base: identity (id,
api_version), capability declaration, and action resolution entry points.
Phase 6 of the execution plan fills this in. Plugins must never depend on
Omega or MeTTa.
"""
