"""Human-readable campaign projections.

Campaign directory layout (campaign.yaml, state/, world/, rulings/,
sessions/, gm/) rendered as derived operator artifacts. SQLite plus the event
log remain the only authority. Phase 13 of the execution plan fills this in.
"""
