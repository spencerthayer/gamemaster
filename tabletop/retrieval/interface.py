"""Retriever interface.

``Retriever.search(query, filters, limit)`` with metadata filtering and
namespace selection (system, setting, adventure, campaign, rulings,
character, npc). Phase 20 of the execution plan fills this in.
"""
