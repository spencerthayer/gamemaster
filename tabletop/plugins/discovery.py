"""Plugin discovery.

Discovers game-system plugins from configured directories at runtime, without
rebuilding Omega. Executable system plugins and non-executable content packs
are kept separate. No dynamic execution of untrusted uploaded documents.
Phase 7 of the execution plan fills this in.
"""
